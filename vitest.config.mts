import { defineConfig } from 'vitest/config';

// Added in case of need to override the default configuration
// TODO: Prevent pushing without successful tests
export default defineConfig({
  // ...
});